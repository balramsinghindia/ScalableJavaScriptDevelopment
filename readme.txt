First Thing When Going to start a Scalable Java Script Development is to 
1. Install Compass
Install Ruby
Setting up the ruby environment
	$ gem update --system
	$ gem install compass
	$ compass create "Project Name"

2. grunt config.rb diffAll  bower pasted from Unicommerce Website

1. install Grunt's command line interface (CLI) globally
npm install -g grunt-cli

2. install npm globally
sudo npm install

3. 00ABE9
